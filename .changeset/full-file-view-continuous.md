---
"hunkdiff": minor
---

Make "View full file" (Shift+V) render each file as one continuous listing, IDE-style: every unchanged region is pre-expanded seamlessly (no interactive gap rows) while hunk boundaries, `[`/`]` navigation, and hunk notes keep working.
