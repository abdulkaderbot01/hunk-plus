---
"hunkdiff": minor
---

Add a `Git` menu with stage / unstage / discard worktree actions and a `Ctrl+L` (or `g g`) shortcut to suspend the TUI and launch `lazygit` in the repo root. Add a `View full file` toggle (`V`) that swaps each file's hunk-only diff for a synthesized full-file diff built from the per-side source text. Add letter-prefix jumps (and `J` / `K` next/prev-file shortcuts) so the sidebar can be navigated by keyboard.
