---
"hunkdiff": minor
---

Add IDE-style collapsed-gap expansion: gap rows now offer `[↓ 20]`, `[↑ 20]`, and `[↕ all]` chips to reveal unchanged context 20 lines at a time from either edge (like VS Code / GitHub), with the remaining hidden-line count kept on the gap row. Clicking the gap label still expands the whole gap, and the `z` shortcut keeps its full expand/collapse toggle.
